# Audio placeholders

The current `.mp3` files are temporary silent placeholders for Step 1.4.

They are copied from `silent-mp3-datauri@1.0.0` (`silence.mp3`, MIT license) so the
MVP can verify asset paths before real word pronunciation files are prepared.

Replace these files with reviewed word pronunciation audio before user testing.
